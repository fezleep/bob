NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Optional. Leave unset for host-only auth cookies.
# Set only when intentionally sharing auth across a custom/root domain and subdomains.
BOB_AUTH_COOKIE_DOMAIN=
PLAYWRIGHT_PORT=3001
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001
